<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Button DockPanel.Dock="Top" Content="Load Temperature" Command="{Binding GetTempCommand}"/>
        <StackPanel Grid.Row="4" Grid.Column="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Date :"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Temperature in Celsius :"/>

                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentTemp.Temperature}"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CurrentTemp.DateTime}"/>
            </Grid>
        </StackPanel>
    </DockPanel>
</Window>
